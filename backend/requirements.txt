# MENAOS backend — runtime dependencies
# FastAPI + SQLAlchemy 2.0 + Alembic + PostgreSQL 16 (psycopg 3), Python 3.12, synchronous.
# Versions web-checked against the PyPI registry on 2026-06-04 (latest stable).

fastapi==0.136.3
uvicorn[standard]==0.49.0

sqlalchemy==2.0.50
alembic==1.18.4
psycopg[binary]==3.3.4

pydantic==2.13.4
pydantic-settings==2.14.1

python-multipart==0.0.30
httpx==0.28.1

pyjwt[crypto]==2.13.0
boto3==1.43.22

cachetools==7.1.4
gunicorn==26.0.0
